# SOAR Playbook

Scripts, runbooks, experiment records, and submission templates for SOAR GPU validation.

## Layout

- `scripts/`: host-side and container-side automation scripts.
- `docs/`: runbook, code map, and experiment table.
- `runs/`: per-run logs and artifacts. Keep large outputs out of git.
- `submission/`: submission package templates.
